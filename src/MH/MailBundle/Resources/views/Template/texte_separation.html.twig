                    <!-- Debut bloc texte separation -->

                    <tr bgcolor="#{{ post.texte.couleurFond }}">
                        <td height="{{ post.texte.hauteur }}">&nbsp;</td>
                    </tr>
                    <tr bgcolor="#{{ post.texte.couleurFond }}">
                        <td style="font-size: 16px; font-family: Arial, sans-serif; color:#{{ post.texte.texte.couleur }}" align="center" valign="middle"><strong>{{ post.texte.texte.paragraphes | first }}<br />
                            </strong></td>
                    </tr>
                    <tr bgcolor="#{{ post.texte.couleurFond }}">
                        <td height="{{ post.texte.hauteur }}">&nbsp;</td>
                    </tr>

                    <!-- Fin bloc texte separation -->